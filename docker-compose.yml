version: '3.8'

services:
  neo4j:
    image: neo4j:5.21.0
    container_name: rdss-neo4j
    ports:
      - "7474:7474"
      - "7687:7687"
    environment:
      NEO4J_AUTH: neo4j/password
      NEO4J_PLUGINS: '["apoc"]'
      NEO4J_apoc_export_file_enabled: "true"
      NEO4J_apoc_import_file_enabled: "true"
      NEO4J_dbms_security_procedures_unrestricted: "apoc.*"
    volumes:
      - ./data/neo4j/data:/data
      - ./data/neo4j/logs:/logs
      - ./data/neo4j/import:/var/lib/neo4j/import
      - ./data/neo4j/plugins:/plugins

  mongodb:
    image: mongo:7.0
    container_name: rdss-mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: rdss_documents
    volumes:
      - ./data/mongo/db:/data/db
      - ./mongodb-init.js:/docker-entrypoint-initdb.d/mongo-init.js:ro

  # redis:
  #   image: redis:7.2
  #   container_name: rdss-redis
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - ./data/redis:/data

# Ollama is expected to run on the host machine for GPU access.
# If running in container is required, uncomment below (CPU only by default unless --gpus passed)
#  ollama:
#    image: ollama/ollama:latest
#    container_name: rdss-ollama
#    ports:
#      - "11434:11434"
#    volumes:
#      - ./data/ollama:/root/.ollama
